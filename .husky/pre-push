export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
# Fall back gracefully if nvm is not installed
command -v nvm >/dev/null 2>&1 && nvm use --silent
npm run lint && npm test
